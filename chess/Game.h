#pragma once
#include "SDL2/SDL.h";

class Game
{

public:
	Game();

	const int TILE_SIZE = 80;
	const SDL_Color whiteColor = { 255, 255, 255 };
	const SDL_Color blackColor = { 10, 10, 10 };


private:

};

